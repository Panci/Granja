# Copia este archivo a .env y sustituye ambos secretos antes de ejecutar npm start.
# Genera SESSION_SECRET con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_PASSWORD=cambia-esta-contrasena-por-una-larga
SESSION_SECRET=cambia-este-secreto-por-64-caracteres-aleatorios
DB_PATH=./data/erp_animal.db
HOST=127.0.0.1
PORT=8080
